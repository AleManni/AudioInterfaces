//
//  ViewController.swift
//  AudioInterfaces
//
//  Created by Carolina Gigler on 12/06/2016.
//  Copyright © 2016 Alessandro Manni. All rights reserved.
//

import UIKit

class ViewController: UIViewController {
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
//        let knob = NSBundle.mainBundle().loadNibNamed("KnobView", owner: self, options: nil)[0] as! KnobView
//        knob.frame = CGRectMake(0, 0, self.view.bounds.size.width, self.view.bounds.size.height)
//        self.view.addSubview(knob)
//        
//        let topConstraint = NSLayoutConstraint(item: knob, attribute: .Top, relatedBy: .Equal, toItem: self.view, attribute: .Top, multiplier: 1, constant: -24)
//        let bottomConstraint = NSLayoutConstraint(item: knob, attribute: .Bottom, relatedBy: .Equal, toItem: self.view, attribute: .Bottom, multiplier: 1, constant: -24)
//        let leadingConstraint = NSLayoutConstraint(item: knob, attribute: .Leading, relatedBy: .Equal, toItem: self.view, attribute: .Leading, multiplier: 1, constant: -24)
//        let trailingConstraint = NSLayoutConstraint(item: knob, attribute: .Trailing, relatedBy: .Equal, toItem: self.view, attribute: .Trailing, multiplier: 1, constant: -24)
//        self.view.addConstraints([topConstraint, bottomConstraint, leadingConstraint, trailingConstraint])
//        
//        
//
//        self.view.layoutIfNeeded()
        
    }
    
    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    
    /*
     // MARK: - Navigation
     
     // In a storyboard-based application, you will often want to do a little preparation before navigation
     override func prepareForSegue(segue: UIStoryboardSegue, sender: AnyObject?) {
     // Get the new view controller using segue.destinationViewController.
     // Pass the selected object to the new view controller.
     }
     */
    
}
